
const initialState = {
    isLoading:true
}

export type AppActionType = {
    isLoading:boolean
}

export const appReducer = (state = initialState,action:AppActionType) => {
    switch (action.type) {

    }
}


